# presentation_template

```
$ git clone --recursive https://github.com/tscholak/presentation_template.git new_presentation
$ cd new_presentation
$ npm init
$ npm install grunt grunt-contrib-qunit grunt-contrib-jshint grunt-contrib-cssmin grunt-contrib-uglify grunt-contrib-watch grunt-sass grunt-contrib-connect grunt-autoprefixer grunt-zip --save-dev
$ grunt serve
```

